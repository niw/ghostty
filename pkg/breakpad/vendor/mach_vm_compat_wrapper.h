#ifndef MACH_VM_COMPAT_WRAPPER_H
#define MACH_VM_COMPAT_WRAPPER_H

// Include our compatibility definitions first
#include "mach_vm_compat.h"

// Then include the system header
#include <mach/mach_vm.h>

#endif // MACH_VM_COMPAT_WRAPPER_H